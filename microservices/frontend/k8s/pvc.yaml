apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ecommerce-assets-pvc
  namespace: ecommerce
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi