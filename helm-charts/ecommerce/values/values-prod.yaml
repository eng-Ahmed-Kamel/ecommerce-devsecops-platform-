backend:
  replicaCount: 2
  resources:
    requests:
      memory: "512Mi"
      cpu: "500m"
    limits:
      memory: "1Gi"
      cpu: "1000m"

frontend:
  replicaCount: 2

hpa:
  enabled: true
  minReplicas: 3
  maxReplicas: 20

ingress:
  host: "your-production-domain.com"